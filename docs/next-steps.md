# Next implementation steps

1. **Real requirements adapter — implemented.** Read-only Jira issue and Coda requirement-row ingestion with document/table references, source revisions, criterion provenance, and repeatable provider fixtures. See [configuration and limits](requirements-ingestion.md). Live account verification remains environment-dependent.
2. **Structured planning provider — implemented.** Optional OpenAI Responses adapter with pinned plan/v2 prompt/schema, local JSON Schema and traceability validation, persisted model/version/hashes, bounded retries/timeouts, and no-call reports for missing requirements. See [configuration and limits](structured-planning.md). Live API verification remains environment-dependent.
3. **Playwright execution adapter — implemented.** Reviewed, hash-bound execution manifests generate fixed Playwright tests in isolated run directories, enforce exact target origins and timeouts, and persist real TestRun status plus local evidence. See [workflow and limits](playwright-execution.md).
4. **MinIO artifact adapter — implemented.** Optional evidence uploads with SHA-256/content types, independent run upload status, resumable publishing, failure-evidence associations, explicit bucket initialization and prefix-scoped retention. See [setup and limits](minio-artifacts.md). Real local MinIO round trips and CLI publishing verified.
5. **Regression promotion — implemented.** Isolated reviewable Git patches for passing non-synthetic plans, stable regression IDs and source/evidence mappings, explicit regression suite and failure triage/review. Assertions are preserved; existing regressions cannot be overwritten. See [workflow and limits](regression-promotion.md).
6. **Operational hardening — implemented; deployment verification pending.** Ordered PostgreSQL migrations are implemented and verified against a real database; see [migration operation](database-migrations.md). API bearer-key authentication is implemented; see [configuration](api-authentication.md). Idempotent API/CLI submissions are implemented for both stores; see [semantics and limits](idempotent-submissions.md). Provider-operation recovery is implemented with persisted starts/results, safe read retries, and review-required uncertain planning outcomes; see [recovery behavior](provider-recovery.md). Worker lease renewal is implemented with coordinated checkpoints and cancellation on renewal failure; see [configuration and behavior](lease-renewal.md). Durable retry budgets are implemented with bounded worker/import attempts and a processing deadline; see [policy and configuration](retry-budgets.md). Job cancellation is implemented across stores, workers, HTTP and CLI; see [behavior and limits](job-cancellation.md). Operational metrics are implemented with authenticated API and optional worker scrapes, bounded counters/gauges/histograms, and process-isolated verification; see [setup and semantics](metrics.md).
7. **Deployment verification — local checks passed; remote CI pending.** The Linux arm64 candidate builds, has zero HIGH/CRITICAL scan findings, and passes container, browser, persistence, and integration checks. See [evidence and remaining gates](deployment-verification.md). The user approved uploading a verification branch; remote CI remains to be completed. Registry publication and a registry-qualified deployment digest remain pending; no deployment was performed. Live provider verification still requires configured credentials.
